# 2026-08-10

## ai-coding-spec 脚手架
- 在 `engineering/` 下建立三套基础工程骨架（结构展示，非可运行）：
  - `frontend/` = React 18 + Vite 5 + TS + Vitest（含 package.json/vite.config.ts/tsconfig.json/index.html/README）
  - `java/` = Spring Boot 3.3 + Maven + Java 17（含 pom.xml/application.yml/README，分层 controller/service/repository/model/config）
  - `python/` = FastAPI + Pydantic v2 + Uvicorn + pytest（含 pyproject.toml/requirements.txt/README，app/{api,models,services,core}）
- 编写 5 份规范文档（重点交付物）：编码规范、agent规范、技能规范、测试规范、文档规范。
- 根 README.md 重写为总览+索引。
- 约定：工程目录仅结构展示，深度=只写规范文档；三栈选择来自用户 AskUserQuestion 答复。

## 重构：规范目录改英文名称
- 将 5 份规范从 `engineering/` 顶层中文文件，移入英文目录（各含 README.md）：
  - `engineering/coding/`、`engineering/agent/`、`engineering/skill/`、`engineering/testing/`、`engineering/documentation/`
- 文档规范目录用 `documentation` 而非 `docs`，避免与自身约定的 `docs/api`、`docs/adr` 冲突。
- 同步更新所有交叉引用：5 份文档 footer、文档内《xxx》引用、三模块 README 路径、根 README 目录树与索引。
- 已 grep 确认无残留中文文件名 / 《》引用。

## 修正：规范目录提升到与 engineering 同级
- 用户要求 5 份规范目录与 `engineering/` 同级（而非嵌套在 engineering 内）。
- 已将 `coding/`、`agent/`、`skill/`、`testing/`、`documentation/` 移到仓库根目录，与 `engineering/` 并列。
- 修正引用：根 README 目录树+索引路径；三模块 README 的 `../coding/README.md` → `../../coding/README.md`（多爬一层出 engineering）；5 份文档互链仍用 `../`（同目录兄弟，无需改）。
- 现状：根级 6 个目录 = engineering/ + 5 个规范目录。
